


// counter 
const initialState = 0;


// counter reducer
export const ColorReducer = (state = initialState, { type, payload }) => {

    switch (type) {
        case '':
            
            break;
    
        default:
            return state;
    }

}
